import { AppShell } from "@/components/AppShell";

/**
 * `/` — the canonical two-column layout (builder beside a sticky review).
 */
export default function Home() {
  return <AppShell variant="main" />;
}
